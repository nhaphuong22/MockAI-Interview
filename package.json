{
  "name": "mockai-interview-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root management for AI Mock Interview Platform",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm run dev --prefix backend\" \"pnpm run dev --prefix frontend\"",
    "install-all": "pnpm install",
    "migrate": "pnpm run migrate --prefix backend && pnpm run seed --prefix backend",
    "migrate:rollback": "pnpm run migrate:rollback --prefix backend",
    "build": "pnpm run build --prefix frontend",
    "lint": "pnpm run lint --prefix frontend",
    "test": "pnpm run test --prefix backend && pnpm run test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
