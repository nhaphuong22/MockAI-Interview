{
  "name": "mockai-interview-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root management for AI Mock Interview Platform",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "migrate": "npm run migrate --prefix backend && npm run seed --prefix backend",
    "migrate:rollback": "npm run migrate:rollback --prefix backend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
