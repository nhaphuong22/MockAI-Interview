{
  "name": "mockai-interview-root",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "description": "Root management for AI Mock Interview Platform",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm -C backend run dev\" \"pnpm -C frontend run dev\"",
    "install-all": "pnpm install",
    "migrate": "pnpm -C backend run migrate && pnpm -C backend run seed",
    "migrate:rollback": "pnpm -C backend run migrate:rollback",
    "build": "pnpm -C frontend run build",
    "lint": "pnpm -C frontend run lint",
    "test": "pnpm -C backend run test && pnpm -C frontend run test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
